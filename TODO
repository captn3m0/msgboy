- Add images/icons to notifications
- Allow of notification grouping when more than 3 are opened
- Options :
 * sound
 * open tabs directly [in the back?]
 * Add a callback so that sites themslves can hook up subscriptions directly!
 * Handle gracefuly when there is no more space for storage!
 * Do not trust the feed's title. Use the page title, as 
 * Ask the user to which feed he wants to subscribe when there are multiple
 * See if we could do sticky notifications
 * Allow for subscirption when the page _is_ a feed.
 * Remember Bookmark position
 